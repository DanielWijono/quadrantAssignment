# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'quadrantAssignment' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Moya', '~> 15.0'
  pod 'Charts', '~> 3.6.0'
  # Pods for quadrantAssignment

  target 'quadrantAssignmentTests' do
    inherit! :search_paths
    pod 'Quick', '~> 4.0.0'
    pod 'Nimble', '~> 9.2.1'
    # Pods for testing
  end

  target 'quadrantAssignmentUITests' do
    # Pods for testing
  end

end
